class Solution {
public:
    int largestElement(vector<int>& nums) {
        int n=nums.size();
        int lar=nums[0];

        for (int i=0;i<n;i++) {
            if (nums[i]>lar) {
                lar=nums[i];
            }
        }
        return lar;
    }
};
